<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mostainahmed.discoverkhulna">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <permission
        android:name="com.mostainahmed.discoverkhulna.maps.MapsActivity2.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.mostainahmed.discoverkhulna.maps.MapsActivity2.permission.MAPS_RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dc_office"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".banks.BankCatagories"></activity>
        <activity android:name=".banks.privatebank.PrivateBankActivity" />
        <activity android:name=".transportation.Transportation" />
        <activity android:name=".touroperators.TourOperators" />
        <activity android:name=".seurityservices.SecurityServices" />
        <activity android:name=".hotel.HotelDetailActivity" />
        <activity android:name=".banks.nationalbank.BankActivity" />
        <activity android:name=".DialerActivity" />
        <activity android:name=".website.WebListActivity" />
        <activity android:name=".MainActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".maps.MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".hotel.HotelActivity" />
        <activity
            android:name=".maps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".website.WebActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".places.PlacesActivity" />
        <activity android:name=".policestation.PoliceStationActivity" />
        <activity android:name=".fireservice.FireServiceActivity" />
        <activity android:name=".hospital.HospitalActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LanguageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>